# Ralph Progress Log
Started: 2026-03-07T15:48:58.002Z
---

## Codebase Patterns

## 2026-03-07 - US-001
- Implemented: configFile property, loadGlobalConfig(), saveGlobalConfig(), getOpenRouterKey(), getEnabledModels() in Config class; .ralph/.gitignore with config.json and .openrouter-models-cache.json
- Files: src/core/config.js, src/core/config.test.js, .ralph/.gitignore
- Learnings: Config class uses existsSync + readFileSync pattern consistently; optional chaining (?.) works well for nested config access with fallbacks; TDD with node:test requires no async in before() for sync setup
---

## 2026-03-07 - US-002
- Implemented: validateApiKey(), fetchAvailableModels(), fetchModelsWithCache() in src/core/openrouter.js using Node.js built-in fetch; cache TTL 1h with JSON cache file
- Files: src/core/openrouter.js (new), src/core/openrouter.test.js (new)
- Learnings: mock.fn() + mock.mockImplementationOnce() works well for mocking globalThis.fetch in node:test; set globalThis.fetch BEFORE dynamic import of module under test; cache pattern: existsSync + readFileSync + JSON.parse with fallthrough on error
---

## 2026-03-07 - US-003
- Implemented: spawnOpenRouter() and runResearch() now instantiate Config(findPrdDir()) and call getOpenRouterKey() to resolve API key from config.json first, falling back to OPENROUTER_API_KEY env var
- Files: src/core/runner.js (modified), src/core/runner.test.js (new)
- Learnings: runner.js imports are ESM with .js extensions; findPrdDir() from ../utils.js works with no args to resolve cwd-relative .ralph/; Config.getOpenRouterKey() already handles env var fallback so no extra logic needed in runner
---

## 2026-03-07 - US-004
- Implemented: ralph_openrouter_config (get/set config with masked apiKey), ralph_openrouter_models (list enabled models), ralph_openrouter_fetch_models (fetch from API), ralph_openrouter_toggle_model (enable/disable model by id) in src/mcp/server.js
- Files: src/mcp/server.js (modified)
- Learnings: MCP tool handlers use try/catch via outer handleTool wrapper; dynamic import('../core/openrouter.js') works inside async switch cases; loadGlobalConfig/saveGlobalConfig pattern cleanly handles nested config upserts
---

## 2026-03-07 - US-005
- Implemented: WorktreeManager class with create/remove/list/getConfig/startRun/startAll/merge/ensureGitignore methods; registry CRUD via .ralph/worktrees.json; live status detection from lock/status files; pattern copying to worktree .ralph/
- Files: src/core/worktree.js (new), src/core/worktree.test.js (new)
- Learnings: Mockable internal methods (_exec, _spawn, _isProcessRunning) enable testing git/process operations without real git worktrees; Config class reuse for worktree .ralph/ dirs works cleanly; dirname(prdDir) reliably derives projectRoot from .ralph/ path
---

## 2026-03-08 - US-006
- Implemented: worktreeCommand(args) in src/commands/worktree.js with subcommands: create, remove/rm, list/ls, status, run, run-all, merge; integrated into src/cli.js with 'worktree' case and HELP text update
- Files: src/commands/worktree.js (new), src/cli.js (modified)
- Learnings: ESM-only project — no require() allowed, use top-level import { join } from 'path'; WorktreeManager.list() provides status info for display; printWorktreeStatus() wraps Config per worktree to show PRD progress bar; run-all output uses printRunAllStatus() for consolidated view
---
## 2026-03-08 - US-007
- Implemented: ralph_worktree_create, ralph_worktree_remove, ralph_worktree_list, ralph_worktree_run, ralph_worktree_run_all, ralph_worktree_create_prd, ralph_worktree_merge tools in src/mcp/server.js
- Files: src/mcp/server.js (modified)
- Learnings: WorktreeManager imported via dynamic import('../core/worktree.js') inside async case handlers; ralph_worktree_create_prd reuses the same prdData shape as ralph_create_prd but targets a worktree's Config via wm.getConfig(name); all 7 tools follow the try/catch handleTool pattern with compact JSON responses
---
## 2026-03-08 - US-009
- Implemented: Updated addStory() in vscode/src/commands/stories.ts to load enabled OpenRouter models from config.json via loadGlobalConfig(); QuickPick shows built-in models (sonnet/opus/haiku) with "Anthropic (built-in)" description plus enabled OpenRouter models with "OpenRouter" description; OpenRouter model selections are stored with 'openrouter:' prefix in story.model; StoryItem description already showed model name (no change needed); types.ts and config.ts already had OpenRouterConfig and loadGlobalConfig() from US-008
- Files: vscode/src/commands/stories.ts (modified)
- Learnings: QuickPick items can be extended with custom fields using intersection types (vscode.QuickPickItem & { modelValue: string }); loadGlobalConfig() was already available on RalphConfig from US-008; stories-tree.ts StoryItem.description already included model name at line 27 — no additional changes needed for model badges
---

## 2026-03-07 - US-008
- Implemented: OpenRouterPanel singleton webview in vscode/src/views/openrouter-panel.ts with API key input (password toggle), validate/save buttons, fetch models from OpenRouter API, model list with enable/disable checkboxes, default/research model dropdowns; added loadGlobalConfig()/saveGlobalConfig() to RalphConfig and GlobalConfig/OpenRouterConfig/OpenRouterModel interfaces to types.ts; registered ralph.openRouterSettings command in extension.ts and package.json with settings-gear button in Stories view title bar
- Files: vscode/src/views/openrouter-panel.ts (new), vscode/src/ralph/config.ts (modified), vscode/src/ralph/types.ts (modified), vscode/src/extension.ts (modified), vscode/package.json (modified), .ralph/prd.json (modified)
- Learnings: DashboardPanel singleton pattern: static show() checks instance, static instance field, private constructor, onDidDispose clears instance; webview communicates with extension via postMessage/onDidReceiveMessage; VS Code theme variables (--vscode-*) ensure consistent styling; panel reads/writes config.json via RalphConfig.loadGlobalConfig()/saveGlobalConfig()
---

## 2026-03-07 - US-010
- Implemented: WorktreesTreeProvider (TreeDataProvider) in vscode/src/views/worktrees-tree.ts showing worktrees with name, branch, status icon (idle/running/complete), progress percentage; context menus for Start/Stop/Merge/Open Folder/Remove; header Create Worktree button with multi-step input (name, branch); registered ralph.worktrees view in package.json; added loadWorktrees() and enrichWorktreeEntry() to RalphConfig; WorktreeEntry/WorktreeInfo interfaces in types.ts; worktrees.json FileSystemWatcher + poll-based auto-refresh; 6 worktree commands wired in extension.ts
- Files: vscode/src/views/worktrees-tree.ts (new), vscode/src/ralph/types.ts (modified), vscode/src/ralph/config.ts (modified), vscode/package.json (modified), vscode/src/extension.ts (modified)
- Learnings: WorktreesTreeProvider follows same pattern as StoriesTreeProvider (EventEmitter, refresh(), updateConfig()); enrichWorktreeEntry reads each worktree's .ralph/status.txt and .ralph/prd.json for live status and progress; worktree commands delegate to ralph CLI via terminal.sendText() matching the loop start/stop pattern; process.kill(pid, 0) throws if process doesn't exist — catch to detect not-running
---
