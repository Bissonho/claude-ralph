# Ralph Progress Log
Started: 2026-03-07T15:48:58.002Z
---

## Codebase Patterns

## 2026-03-07 - US-001
- Implemented: configFile property, loadGlobalConfig(), saveGlobalConfig(), getOpenRouterKey(), getEnabledModels() in Config class; .ralph/.gitignore with config.json and .openrouter-models-cache.json
- Files: src/core/config.js, src/core/config.test.js, .ralph/.gitignore
- Learnings: Config class uses existsSync + readFileSync pattern consistently; optional chaining (?.) works well for nested config access with fallbacks; TDD with node:test requires no async in before() for sync setup
---

## 2026-03-07 - US-002
- Implemented: validateApiKey(), fetchAvailableModels(), fetchModelsWithCache() in src/core/openrouter.js using Node.js built-in fetch; cache TTL 1h with JSON cache file
- Files: src/core/openrouter.js (new), src/core/openrouter.test.js (new)
- Learnings: mock.fn() + mock.mockImplementationOnce() works well for mocking globalThis.fetch in node:test; set globalThis.fetch BEFORE dynamic import of module under test; cache pattern: existsSync + readFileSync + JSON.parse with fallthrough on error
---

## 2026-03-07 - US-003
- Implemented: spawnOpenRouter() and runResearch() now instantiate Config(findPrdDir()) and call getOpenRouterKey() to resolve API key from config.json first, falling back to OPENROUTER_API_KEY env var
- Files: src/core/runner.js (modified), src/core/runner.test.js (new)
- Learnings: runner.js imports are ESM with .js extensions; findPrdDir() from ../utils.js works with no args to resolve cwd-relative .ralph/; Config.getOpenRouterKey() already handles env var fallback so no extra logic needed in runner
---

## 2026-03-07 - US-004
- Implemented: ralph_openrouter_config (get/set config with masked apiKey), ralph_openrouter_models (list enabled models), ralph_openrouter_fetch_models (fetch from API), ralph_openrouter_toggle_model (enable/disable model by id) in src/mcp/server.js
- Files: src/mcp/server.js (modified)
- Learnings: MCP tool handlers use try/catch via outer handleTool wrapper; dynamic import('../core/openrouter.js') works inside async switch cases; loadGlobalConfig/saveGlobalConfig pattern cleanly handles nested config upserts
---

## 2026-03-07 - US-005
- Implemented: WorktreeManager class with create/remove/list/getConfig/startRun/startAll/merge/ensureGitignore methods; registry CRUD via .ralph/worktrees.json; live status detection from lock/status files; pattern copying to worktree .ralph/
- Files: src/core/worktree.js (new), src/core/worktree.test.js (new)
- Learnings: Mockable internal methods (_exec, _spawn, _isProcessRunning) enable testing git/process operations without real git worktrees; Config class reuse for worktree .ralph/ dirs works cleanly; dirname(prdDir) reliably derives projectRoot from .ralph/ path
---
