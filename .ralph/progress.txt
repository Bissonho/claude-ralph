# Ralph Progress Log
Started: 2026-03-07T16:49:21.580Z
---

## 2026-03-08 - SUM-001
- Implemented: Simple sum function that adds two integers
- Files: src/test-sums/sum01.js, src/test-sums/sum01.test.js
- Learnings: ESM imports with .js extensions, Node.js test runner with node:test module, TDD approach with failing tests first
---

## 2026-03-08 - SUM-002
- Implemented: Array summing function using reduce to sum all elements
- Files: src/test-sums/sum02.js, src/test-sums/sum02.test.js
- Learnings: Array.reduce() pattern for aggregations, handle edge cases (empty arrays return 0), all 3 acceptance criteria pass
---

## 2026-03-08 - SUM-003
- Implemented: Sum of even numbers using filter and reduce pattern
- Files: src/test-sums/sum03.js, src/test-sums/sum03.test.js
- Learnings: Combining filter() and reduce() for conditional aggregations, TDD approach validates all acceptance criteria efficiently
---

## 2026-03-08 - SUM-004
- Implemented: Sum of odd numbers using filter and reduce pattern
- Files: src/test-sums/sum04.js, src/test-sums/sum04.test.js
- Learnings: Odd number check with num % 2 !== 0, all 3 acceptance criteria pass via TDD
---

## 2026-03-08 - RES-001
- Implemented: stderr capture in spawnProcess() — stdio changed from ['pipe','pipe','inherit'] to ['pipe','pipe','pipe'], stderr chunks accumulated into result.stderr string, printed to process.stderr in real-time
- Files: src/core/runner.js, src/core/runner.test.js
- Learnings: Real-time stderr visibility preserved via process.stderr.write(); result shape is {output, stderr, code, killed}; all 91 tests pass with TDD approach
---

## 2026-03-08 - RES-002
- Implemented: classifyError(exitCode, stderr, killed=false) in src/core/runner.js — returns {type, retryable, message}; types: auth (non-retryable), network, rate_limit, timeout, killed (non-retryable), unknown (retryable)
- Files: src/core/runner.js, src/core/runner.test.js
- Learnings: TDD with 17 failing tests first; auth check before rate_limit to avoid 401/403 false positives; case-insensitive stderr matching; exit code 124 for timeout; all 26 tests pass
---

## 2026-03-08 - RES-003
- Implemented: retryWithBackoff() exported from run.js — wraps spawnFn with up to 3 retries using BACKOFF_DELAYS [5000,15000,45000]ms; classifyError() drives retry/skip decision; countdown logs every 5s chunk; main loop uses retryWithBackoff() instead of direct spawnAgent(); exhausted retries mark story failed=true; getNextStory() in config.js updated to also filter !s.failed
- Files: src/commands/run.js, src/commands/run.test.js, src/core/config.js
- Learnings: TDD with 9 failing tests first; injectable sleepFn/logFn/classifyFn enables deterministic unit tests without real waits; sleep chunking in 5s intervals produces countdown messages naturally; getNextStory must filter failed stories to prevent re-picking exhausted ones; 117 tests all pass
---

## 2026-03-08 - RES-004
- Implemented: checkAutoPause(failures) exported from run.js — tracks consecutive story failures; pauses after 3 consecutive same non-retryable type OR 5 consecutive any type; setPauseState/getPauseState in config.js write/read .ralph/pause.json with {reason, pausedAt, lastStoryId, consecutiveFailures}; exitFn option added to run() for testability; consecutiveFailures array resets on story success; status.txt set to 'paused: <reason>' before exit code 2
- Files: src/commands/run.js, src/commands/run.test.js, src/core/config.js, src/core/config.test.js
- Learnings: checkAutoPause as pure exported function enables easy unit testing without process.exit mocking; injectable exitFn pattern (like sleepFn) allows integration-level testing; consecutiveFailures.length = 0 (vs reassign) mutates in place; non-retryable check should only apply to rule #1 (3 consecutive same type), rule #2 (5 any type) includes retryable failures too; call releaseLock() explicitly before exitFn(2) since finally block doesn't run after process.exit
---

## Codebase Patterns
